# This file is used to configure the 3D sensors for the robot.
# Depth camera sensors are disabled because no depth cameras are connected.
# MoveIt2 will still perform collision checking using the robot's URDF model.
sensors: []

# Depth camera configuration (disabled - no hardware available)
# Uncomment and configure if you add a depth camera (Intel RealSense, Kinect, etc.)
#
# default_sensor:
#   far_clipping_plane_distance: 5.0
#   filtered_cloud_topic: filtered_cloud
#   image_topic: /camera/depth/image_raw
#   max_update_rate: 1.0
#   near_clipping_plane_distance: 0.3
#   padding_offset: 0.03
#   padding_scale: 4.0
#   queue_size: 5
#   sensor_plugin: occupancy_map_monitor/DepthImageOctomapUpdater
#   shadow_threshold: 0.2